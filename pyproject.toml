[project]
name = "psych-team-395"
version = "0.1.0"
description = "Simulation dashboard with Flask backend and vanilla JS frontend."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "flask-cors",
    "flask-login>=0.6",
    "flask-sqlalchemy>=3.0",
    "requests",
    "python-dotenv",
]

[tool.uv]
package = false
